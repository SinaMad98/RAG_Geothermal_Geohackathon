# Core dependencies for Oil & Gas Well Report Data Extraction
# Standard library modules are used, no external dependencies required for basic functionality

# Optional: For data validation against JSON schema
jsonschema>=4.0.0

# Optional: For enhanced data processing
python-dateutil>=2.8.0
